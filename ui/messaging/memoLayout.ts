export const MEMO_FIELD_MIN_HEIGHT_PX = 108;
// ZEC memo field uses textarea autosize logic and needs a slightly larger lift.
export const ZEC_MEMO_AMOUNT_OVERLAP_PX = 57.25;
// non-ZEC disabled memo block is fixed-height and aligns lower with a smaller lift.
export const NON_ZEC_MEMO_AMOUNT_OVERLAP_PX = 52;
